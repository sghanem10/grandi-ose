/**
 * Grandi-Ose ! — point d'entree JavaScript
 *
 * Le site d'origine ne contenait AUCUN JavaScript, et la phase 1 est un refactor
 * a rendu identique : ce fichier est donc volontairement sans comportement.
 * Il n'existe que pour que la structure soit en place et charge sans erreur.
 *
 * Le depliant « Lire la suite de mon histoire » fonctionne sans JavaScript
 * (element <details> natif), et le defilement doux est gere en CSS
 * (scroll-behavior: smooth). Rien a reprendre ici.
 *
 * Modules a venir (voir ROADMAP.md) :
 *   modules/form.js    phase 4  soumission du formulaire, etats, validation
 *   modules/nav.js     phase 6  menu mobile — il n'y a aujourd'hui AUCUNE
 *                               navigation sous 860px
 *   modules/reveal.js  phase 6  apparition des sections au defilement
 *   modules/year.js    phase 6  annee du pied de page en automatique
 *
 * Charge en type="module", donc differe par defaut et en mode strict.
 */

export {};
